# SSH deploy key — NEVER commit the private key
deploy_key

# macOS
.DS_Store
**/.DS_Store

# Scripts used to build assets (optional to include)
download_images.py
download_audio.sh
download_images.sh
